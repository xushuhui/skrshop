// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"skrshop/internal/data/ent/productskustock"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ProductSkuStockCreate is the builder for creating a ProductSkuStock entity.
type ProductSkuStockCreate struct {
	config
	mutation *ProductSkuStockMutation
	hooks    []Hook
}

// SetCreateAt sets the "create_at" field.
func (pssc *ProductSkuStockCreate) SetCreateAt(t time.Time) *ProductSkuStockCreate {
	pssc.mutation.SetCreateAt(t)
	return pssc
}

// SetUpdateAt sets the "update_at" field.
func (pssc *ProductSkuStockCreate) SetUpdateAt(t time.Time) *ProductSkuStockCreate {
	pssc.mutation.SetUpdateAt(t)
	return pssc
}

// SetStatus sets the "status" field.
func (pssc *ProductSkuStockCreate) SetStatus(i int8) *ProductSkuStockCreate {
	pssc.mutation.SetStatus(i)
	return pssc
}

// SetNillableStatus sets the "status" field if the given value is not nil.
func (pssc *ProductSkuStockCreate) SetNillableStatus(i *int8) *ProductSkuStockCreate {
	if i != nil {
		pssc.SetStatus(*i)
	}
	return pssc
}

// Mutation returns the ProductSkuStockMutation object of the builder.
func (pssc *ProductSkuStockCreate) Mutation() *ProductSkuStockMutation {
	return pssc.mutation
}

// Save creates the ProductSkuStock in the database.
func (pssc *ProductSkuStockCreate) Save(ctx context.Context) (*ProductSkuStock, error) {
	var (
		err  error
		node *ProductSkuStock
	)
	pssc.defaults()
	if len(pssc.hooks) == 0 {
		if err = pssc.check(); err != nil {
			return nil, err
		}
		node, err = pssc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ProductSkuStockMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = pssc.check(); err != nil {
				return nil, err
			}
			pssc.mutation = mutation
			node, err = pssc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(pssc.hooks) - 1; i >= 0; i-- {
			mut = pssc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, pssc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (pssc *ProductSkuStockCreate) SaveX(ctx context.Context) *ProductSkuStock {
	v, err := pssc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// defaults sets the default values of the builder before save.
func (pssc *ProductSkuStockCreate) defaults() {
	if _, ok := pssc.mutation.Status(); !ok {
		v := productskustock.DefaultStatus
		pssc.mutation.SetStatus(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pssc *ProductSkuStockCreate) check() error {
	if _, ok := pssc.mutation.CreateAt(); !ok {
		return &ValidationError{Name: "create_at", err: errors.New("ent: missing required field \"create_at\"")}
	}
	if _, ok := pssc.mutation.UpdateAt(); !ok {
		return &ValidationError{Name: "update_at", err: errors.New("ent: missing required field \"update_at\"")}
	}
	if _, ok := pssc.mutation.Status(); !ok {
		return &ValidationError{Name: "status", err: errors.New("ent: missing required field \"status\"")}
	}
	return nil
}

func (pssc *ProductSkuStockCreate) sqlSave(ctx context.Context) (*ProductSkuStock, error) {
	_node, _spec := pssc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pssc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (pssc *ProductSkuStockCreate) createSpec() (*ProductSkuStock, *sqlgraph.CreateSpec) {
	var (
		_node = &ProductSkuStock{config: pssc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: productskustock.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: productskustock.FieldID,
			},
		}
	)
	if value, ok := pssc.mutation.CreateAt(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: productskustock.FieldCreateAt,
		})
		_node.CreateAt = value
	}
	if value, ok := pssc.mutation.UpdateAt(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: productskustock.FieldUpdateAt,
		})
		_node.UpdateAt = value
	}
	if value, ok := pssc.mutation.Status(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeInt8,
			Value:  value,
			Column: productskustock.FieldStatus,
		})
		_node.Status = value
	}
	return _node, _spec
}

// ProductSkuStockCreateBulk is the builder for creating many ProductSkuStock entities in bulk.
type ProductSkuStockCreateBulk struct {
	config
	builders []*ProductSkuStockCreate
}

// Save creates the ProductSkuStock entities in the database.
func (psscb *ProductSkuStockCreateBulk) Save(ctx context.Context) ([]*ProductSkuStock, error) {
	specs := make([]*sqlgraph.CreateSpec, len(psscb.builders))
	nodes := make([]*ProductSkuStock, len(psscb.builders))
	mutators := make([]Mutator, len(psscb.builders))
	for i := range psscb.builders {
		func(i int, root context.Context) {
			builder := psscb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ProductSkuStockMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, psscb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, psscb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, psscb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (psscb *ProductSkuStockCreateBulk) SaveX(ctx context.Context) []*ProductSkuStock {
	v, err := psscb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
