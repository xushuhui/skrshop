// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"skrshop/internal/data/ent/productattr"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
)

// ProductAttr is the model entity for the ProductAttr schema.
type ProductAttr struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 状态 1:enable, 0:disable, -1:deleted
	Status int8 `json:"status,omitempty"`
	// 创建时间
	CreateAt time.Time `json:"create_at,omitempty"`
	// 更新时间
	UpdateAt time.Time `json:"update_at,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ProductAttr) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case productattr.FieldID, productattr.FieldStatus:
			values[i] = &sql.NullInt64{}
		case productattr.FieldCreateAt, productattr.FieldUpdateAt:
			values[i] = &sql.NullTime{}
		default:
			return nil, fmt.Errorf("unexpected column %q for type ProductAttr", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ProductAttr fields.
func (pa *ProductAttr) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case productattr.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			pa.ID = int(value.Int64)
		case productattr.FieldStatus:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				pa.Status = int8(value.Int64)
			}
		case productattr.FieldCreateAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_at", values[i])
			} else if value.Valid {
				pa.CreateAt = value.Time
			}
		case productattr.FieldUpdateAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_at", values[i])
			} else if value.Valid {
				pa.UpdateAt = value.Time
			}
		}
	}
	return nil
}

// Update returns a builder for updating this ProductAttr.
// Note that you need to call ProductAttr.Unwrap() before calling this method if this ProductAttr
// was returned from a transaction, and the transaction was committed or rolled back.
func (pa *ProductAttr) Update() *ProductAttrUpdateOne {
	return (&ProductAttrClient{config: pa.config}).UpdateOne(pa)
}

// Unwrap unwraps the ProductAttr entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pa *ProductAttr) Unwrap() *ProductAttr {
	tx, ok := pa.config.driver.(*txDriver)
	if !ok {
		panic("ent: ProductAttr is not a transactional entity")
	}
	pa.config.driver = tx.drv
	return pa
}

// String implements the fmt.Stringer.
func (pa *ProductAttr) String() string {
	var builder strings.Builder
	builder.WriteString("ProductAttr(")
	builder.WriteString(fmt.Sprintf("id=%v", pa.ID))
	builder.WriteString(", status=")
	builder.WriteString(fmt.Sprintf("%v", pa.Status))
	builder.WriteString(", create_at=")
	builder.WriteString(pa.CreateAt.Format(time.ANSIC))
	builder.WriteString(", update_at=")
	builder.WriteString(pa.UpdateAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ProductAttrs is a parsable slice of ProductAttr.
type ProductAttrs []*ProductAttr

func (pa ProductAttrs) config(cfg config) {
	for _i := range pa {
		pa[_i].config = cfg
	}
}
