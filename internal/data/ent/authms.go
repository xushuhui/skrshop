// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"skrshop/internal/data/ent/authms"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
)

// AuthMs is the model entity for the AuthMs schema.
type AuthMs struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 状态 1:enable, 0:disable, -1:deleted
	Status int8 `json:"status,omitempty"`
	// 创建时间
	CreateAt time.Time `json:"create_at,omitempty"`
	// 更新时间
	UpdateAt time.Time `json:"update_at,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AuthMs) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case authms.FieldID, authms.FieldStatus:
			values[i] = &sql.NullInt64{}
		case authms.FieldCreateAt, authms.FieldUpdateAt:
			values[i] = &sql.NullTime{}
		default:
			return nil, fmt.Errorf("unexpected column %q for type AuthMs", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AuthMs fields.
func (am *AuthMs) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case authms.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			am.ID = int(value.Int64)
		case authms.FieldStatus:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				am.Status = int8(value.Int64)
			}
		case authms.FieldCreateAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_at", values[i])
			} else if value.Valid {
				am.CreateAt = value.Time
			}
		case authms.FieldUpdateAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_at", values[i])
			} else if value.Valid {
				am.UpdateAt = value.Time
			}
		}
	}
	return nil
}

// Update returns a builder for updating this AuthMs.
// Note that you need to call AuthMs.Unwrap() before calling this method if this AuthMs
// was returned from a transaction, and the transaction was committed or rolled back.
func (am *AuthMs) Update() *AuthMsUpdateOne {
	return (&AuthMsClient{config: am.config}).UpdateOne(am)
}

// Unwrap unwraps the AuthMs entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (am *AuthMs) Unwrap() *AuthMs {
	tx, ok := am.config.driver.(*txDriver)
	if !ok {
		panic("ent: AuthMs is not a transactional entity")
	}
	am.config.driver = tx.drv
	return am
}

// String implements the fmt.Stringer.
func (am *AuthMs) String() string {
	var builder strings.Builder
	builder.WriteString("AuthMs(")
	builder.WriteString(fmt.Sprintf("id=%v", am.ID))
	builder.WriteString(", status=")
	builder.WriteString(fmt.Sprintf("%v", am.Status))
	builder.WriteString(", create_at=")
	builder.WriteString(am.CreateAt.Format(time.ANSIC))
	builder.WriteString(", update_at=")
	builder.WriteString(am.UpdateAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// AuthMsSlice is a parsable slice of AuthMs.
type AuthMsSlice []*AuthMs

func (am AuthMsSlice) config(cfg config) {
	for _i := range am {
		am[_i].config = cfg
	}
}
