// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"skrshop/internal/data/ent/productattrvalue"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ProductAttrValueCreate is the builder for creating a ProductAttrValue entity.
type ProductAttrValueCreate struct {
	config
	mutation *ProductAttrValueMutation
	hooks    []Hook
}

// SetStatus sets the "status" field.
func (pavc *ProductAttrValueCreate) SetStatus(i int8) *ProductAttrValueCreate {
	pavc.mutation.SetStatus(i)
	return pavc
}

// SetNillableStatus sets the "status" field if the given value is not nil.
func (pavc *ProductAttrValueCreate) SetNillableStatus(i *int8) *ProductAttrValueCreate {
	if i != nil {
		pavc.SetStatus(*i)
	}
	return pavc
}

// SetCreateAt sets the "create_at" field.
func (pavc *ProductAttrValueCreate) SetCreateAt(t time.Time) *ProductAttrValueCreate {
	pavc.mutation.SetCreateAt(t)
	return pavc
}

// SetUpdateAt sets the "update_at" field.
func (pavc *ProductAttrValueCreate) SetUpdateAt(t time.Time) *ProductAttrValueCreate {
	pavc.mutation.SetUpdateAt(t)
	return pavc
}

// Mutation returns the ProductAttrValueMutation object of the builder.
func (pavc *ProductAttrValueCreate) Mutation() *ProductAttrValueMutation {
	return pavc.mutation
}

// Save creates the ProductAttrValue in the database.
func (pavc *ProductAttrValueCreate) Save(ctx context.Context) (*ProductAttrValue, error) {
	var (
		err  error
		node *ProductAttrValue
	)
	pavc.defaults()
	if len(pavc.hooks) == 0 {
		if err = pavc.check(); err != nil {
			return nil, err
		}
		node, err = pavc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ProductAttrValueMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = pavc.check(); err != nil {
				return nil, err
			}
			pavc.mutation = mutation
			node, err = pavc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(pavc.hooks) - 1; i >= 0; i-- {
			mut = pavc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, pavc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (pavc *ProductAttrValueCreate) SaveX(ctx context.Context) *ProductAttrValue {
	v, err := pavc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// defaults sets the default values of the builder before save.
func (pavc *ProductAttrValueCreate) defaults() {
	if _, ok := pavc.mutation.Status(); !ok {
		v := productattrvalue.DefaultStatus
		pavc.mutation.SetStatus(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pavc *ProductAttrValueCreate) check() error {
	if _, ok := pavc.mutation.Status(); !ok {
		return &ValidationError{Name: "status", err: errors.New("ent: missing required field \"status\"")}
	}
	if _, ok := pavc.mutation.CreateAt(); !ok {
		return &ValidationError{Name: "create_at", err: errors.New("ent: missing required field \"create_at\"")}
	}
	if _, ok := pavc.mutation.UpdateAt(); !ok {
		return &ValidationError{Name: "update_at", err: errors.New("ent: missing required field \"update_at\"")}
	}
	return nil
}

func (pavc *ProductAttrValueCreate) sqlSave(ctx context.Context) (*ProductAttrValue, error) {
	_node, _spec := pavc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pavc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (pavc *ProductAttrValueCreate) createSpec() (*ProductAttrValue, *sqlgraph.CreateSpec) {
	var (
		_node = &ProductAttrValue{config: pavc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: productattrvalue.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: productattrvalue.FieldID,
			},
		}
	)
	if value, ok := pavc.mutation.Status(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeInt8,
			Value:  value,
			Column: productattrvalue.FieldStatus,
		})
		_node.Status = value
	}
	if value, ok := pavc.mutation.CreateAt(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: productattrvalue.FieldCreateAt,
		})
		_node.CreateAt = value
	}
	if value, ok := pavc.mutation.UpdateAt(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: productattrvalue.FieldUpdateAt,
		})
		_node.UpdateAt = value
	}
	return _node, _spec
}

// ProductAttrValueCreateBulk is the builder for creating many ProductAttrValue entities in bulk.
type ProductAttrValueCreateBulk struct {
	config
	builders []*ProductAttrValueCreate
}

// Save creates the ProductAttrValue entities in the database.
func (pavcb *ProductAttrValueCreateBulk) Save(ctx context.Context) ([]*ProductAttrValue, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pavcb.builders))
	nodes := make([]*ProductAttrValue, len(pavcb.builders))
	mutators := make([]Mutator, len(pavcb.builders))
	for i := range pavcb.builders {
		func(i int, root context.Context) {
			builder := pavcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ProductAttrValueMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pavcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pavcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pavcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pavcb *ProductAttrValueCreateBulk) SaveX(ctx context.Context) []*ProductAttrValue {
	v, err := pavcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
